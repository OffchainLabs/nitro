[package]
name = "hostio-test"
version = "0.1.0"
edition = { workspace = true }

[dependencies]
stylus-sdk = { workspace = true, features = ["debug", "hostio"] }

[features]
contract-client-gen = []
export-abi = ["stylus-sdk/export-abi"]
