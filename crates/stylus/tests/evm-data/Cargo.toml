[package]
name = "evm-data"
version = "0.1.0"
edition.workspace = true

[dependencies]
stylus-sdk = { workspace = true, features = ["debug"] }
hex.workspace = true

[features]
contract-client-gen = []
