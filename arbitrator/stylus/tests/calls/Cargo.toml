[package]
name = "calls"
version = "0.1.0"
edition = "2021"

[dependencies]
arbitrum = { path = "../../../langs/rust/" }
eyre = "0.6.5"
hex = "0.4.3"

[profile.release]
codegen-units = 1
strip = true
lto = true
panic = "abort"

[workspace]
