[package]
name = "brotli"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
paste = {versoion="1.0.14"}
callerenv = { path = "../../callerenv/", features = ["static_caller"] }
