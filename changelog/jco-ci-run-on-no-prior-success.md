### Ignored
- Always run CI on a branch with no prior successful run, even if only changelog files changed
- Skip codecov test-results aggregation when no test jobs ran, instead of failing on missing JUnit reports
