### Added
- New dangerous parameters `--node.bold.dangerous.assume-valid` and `--node.bold.dangerous.assume-valid-hash` to have validator assume all messages up to given message have already been validated

