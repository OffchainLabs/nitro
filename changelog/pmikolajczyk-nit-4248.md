### Added
 - `ValidationInputsAt` debug API now includes an `ExpectedEndState` field in the returned JSON, allowing the arbitrator and JIT provers to verify their computed end state when run from the command line with `--json-inputs`
