### Added
- Hashed address filter implementation for address filter interfaces with shared LRU caching

### Configuration
- Add address-filter.address-checker-worker-count to configure the number of address checker workers
- Add address-filter.address-checker-queue-size to configure the address checker queue capacity
