### Added
- Introduce event filter module for filtering transaction logs based on event selectors and topic-encoded addresses

### Configuration
- Add `execution.sequencer.event-filter.path` to configure sequencer-side event-based transaction filtering via a JSON rules file