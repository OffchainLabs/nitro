### Configuration
- Add `--node.data-availability.rest-aggregator.connection-wait` how long to wait for initial anytrust DA connection until it errors (re-attempts every 1 second) (to be deprecated, use `da.anytrust*` instead)
- Add `--node.da.anytrust.rest-aggregator.connection-wait` how long to wait for initial anytrust DA connection until it errors (re-attempts every 1 second)
- Add `--anytrust.rest-aggregator.connection-wait` how long to wait for initial anytrust DA connection until it errors (re-attempts every 1 second)
