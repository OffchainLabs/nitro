### Ignored
- Skip the merge queue Go test suite when the identical tree already passed PR CI
- Skip the Go test suite in the merge queue when only changelog files changed
- Skip code coverage in the merge queue
