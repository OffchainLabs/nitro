name: CI

on:
  pull_request:
    paths-ignore:
      - 'arbitrator/**'
      - 'nitro-contracts/src/osp/**'
      - 'nitro-contracts/src/mock/**'
      - 'nitro-contracts/test/**'
      - 'nitro-contracts/hardhat.config.ts'
      - 'Makefile'

jobs:
  coverage:
    name: Run Arbitrator tests
    runs-on: ubuntu-latest
    steps:
      - name: Do nothing
        run: echo "doing nothing"
